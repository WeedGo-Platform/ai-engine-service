# OTP Authentication Configuration

# Twilio Configuration (for SMS OTP)
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890  # Your Twilio phone number
TWILIO_VERIFY_SERVICE_SID=your_verify_service_sid_here  # Optional: for Twilio Verify

# SendGrid Configuration (for Email OTP)
SENDGRID_API_KEY=your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=noreply@yourcompany.com
SENDGRID_FROM_NAME=Your Company Name

# OTP Configuration
OTP_LENGTH=6                    # Length of OTP code (default: 6)
OTP_EXPIRY_MINUTES=10           # OTP expiration time in minutes (default: 10)
OTP_MAX_ATTEMPTS=3              # Max verification attempts (default: 3)

# Rate Limiting Configuration
OTP_RATE_LIMIT_MAX=5            # Max OTP requests per window (default: 5)
OTP_RATE_LIMIT_WINDOW=60        # Rate limit window in minutes (default: 60)

# Database Configuration (if different from main config)
DB_HOST=localhost
DB_PORT=5434
DB_NAME=ai_engine
DB_USER=weedgo
DB_PASSWORD=weedgo123

# Security
JWT_SECRET=your-secret-key-here
ENABLE_AUTH=true

# Instructions:
# 1. Copy this file to .env
# 2. Set up Twilio account at https://www.twilio.com
#    - Get Account SID and Auth Token from Console
#    - Purchase a phone number for sending SMS
#    - Optional: Set up Verify service for enhanced security
# 3. Set up SendGrid account at https://sendgrid.com
#    - Create an API key with Mail Send permissions
#    - Verify your sender email address
# 4. Update all values with your actual credentials
# 5. Never commit the actual .env file to version control