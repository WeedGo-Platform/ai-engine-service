# AWS Communication Providers - Additional Requirements

# AWS SDK for Python (SES and SNS)
boto3>=1.28.0
botocore>=1.31.0

# Already in requirements.txt (keeping for reference):
# twilio>=8.0.0  # SMS fallback
# sendgrid>=6.9.0  # Email fallback
# aiohttp>=3.8.0  # HTTP client
